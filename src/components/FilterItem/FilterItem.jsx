import React from 'react';
import styles from "./FilterItem.module.scss";

const FilterItem = () => {
    return (
        <div>
            Filter Item works
        </div>
    )
}

export default FilterItem
