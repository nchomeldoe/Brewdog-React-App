import React from 'react';
import styles from "./SearchBox.module.scss";

const SearchBox = () => {
    return (
        <div>
            Search Box works
        </div>
    )
}

export default SearchBox
